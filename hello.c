/* hello.c */
#include <stdio.h> /*standard input/output*/

/* function : is a named block of code that performs a specific task, it helps to divide program into smaller,
 reusable and organized parts. */

int main()
// funtions are identified by noticing paranthesis "()" directly connected to name
// main function is always present in a c project
{
    printf("Hello World! \n");
    // "\n" this is new-line character it shifts cursor to the next line
    return 0; // this means we are exiting the program(returning "0" also silently means that everything is ok)
}
